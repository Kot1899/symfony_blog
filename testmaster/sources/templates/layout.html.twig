<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">

        <link href="/css/bootstrap.min.css" rel="stylesheet">
        <link href="/css/blog.css" rel="stylesheet">

    </head>
    <body>
        <div class="container">
            <header class="blog-header py-3">
                <div class="row flex-nowrap justify-content-between align-items-center">
                    <div class="col-4 d-flex  justify-content-start align-items-start">
                        <a href="{{ path('homepage') }}">Logo</a>
                        |
                        <a href="{{ path('homepage', { _locale: 'ru' }) }}">RU</a>
                        |
                        <a href="{{ path('homepage', { _locale: 'en' }) }}">EN</a>
                    </div>

                    <div class="col-4">
                        <form action="{{ path('search') }}">
                            <div class="input-group">
                                <input type="text" name="keyword" class="form-control" />
                                <button class="btn btn-outline-secondary" type="submit">Search</button>
                            </div>
                        </form>
                    </div>
                    <div class="col-4 d-flex justify-content-end align-items-center">
                        {% if app.user %}
                            Hello, {{ app.user }}!
                            <a class="btn btn-sm btn-outline-secondary" href="{{ path('logout') }}">Logout</a>
                        {% else %}
                            <a class="btn btn-sm btn-outline-secondary" href="{{ path('login') }}">Login</a>
                        {% endif %}

                    </div>
                </div>

                <h1>{{ 'Hello World'|trans }}</h1>
            </header>

            <div class="nav-scroller py-1 mb-2">
                <nav class="nav d-flex justify-content-start">
                    <a class="p-2 link-secondary" href="{{ path('default_about') }}">{{ 'About'|trans }}</a>
                    <a class="p-2 link-secondary" href="{{ path('default_feedback') }}">{{ 'Feedback'|trans }}</a>
                    <a class="p-2 link-secondary" href="https://facebook.com" target="_blank">{{ 'Facebook'|trans }}</a>
                    <a class="p-2 link-secondary" href="{{ path('post_create') }}">{{ '+ Post'|trans }}</a>
                </nav>
            </div>
        </div>

        <main class="container">
            {% block body %}{% endblock %}
        </main>

        <footer class="blog-footer">
            <p>Blog template built for <a href="https://getbootstrap.com/">Bootstrap</a> by <a href="https://twitter.com/mdo">@mdo</a>.</p>
            <p>
                <a href="#">Back to top</a>
            </p>
        </footer>
    </body>
</html>
