{% extends'layout.html.twig' %}

  {% block body %}
    page with ! edit post
    <h4> Edit the post
        "{{ post.getName }}"
    </h4>

    <div class="row">
      <div class="col-5">
        {{ form(postFormNew) }}
      </div>
    </div>

  {% endblock %}
