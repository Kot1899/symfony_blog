{% extends'layout.html.twig' %}

  {% block body %}
      <div class="row g-5">
          <div class="col-md-8">
              <article class="blog-post">
                  <h2 class="blog-post-title">
                     {{ postId.getName() }} <br/>
                  </h2>
                  <p class="blog-post-meta">
                      {{ postId.getPublicAt()| date('Y-m-d') }}
                      by
                      <a href="{{ path('default_kile') }}">{{ author.getAuthor }}</a>
                      <a href="{{ path('post_editMyPost', {postId: postId.getId}) }}">edit post</a></p>
                  <p>
                      {{ postId.getDescription() }}
                   </article>
          </div>
      </div>
  {% endblock %}



